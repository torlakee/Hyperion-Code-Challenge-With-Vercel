{
  "name": "hyperion-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write .",
    "test": "jest",
    "start:express": "ts-node server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^latest",
    "@vercel/ai": "^latest",
    "axios": "^latest",
    "dotenv": "^latest"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^latest",
    "@typescript-eslint/parser": "^latest",
    "eslint": "^latest",
    "eslint-config-prettier": "^latest",
    "jest": "^latest",
    "prettier": "^latest",
    "ts-jest": "^latest",
    "typescript": "^latest"
  }
}